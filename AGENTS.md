# 项目开发安全规则

- 仅允许在当前仓库目录内创建、读取和修改项目文件。
- 不得修改或删除仓库外的文件。
- 不得修改注册表、系统环境变量、启动项、防火墙或系统服务。
- 不得执行全局软件安装；Python 依赖安装到项目 `.venv`。
- 删除、覆盖或迁移用户已有文件前必须先征得用户同意。
- 密钥只能通过未提交的 `.env` 提供，不得写入代码或提交到 Git。
- 修改前后检查 Git 状态，保留用户已有改动。
- 禁止使用 `git reset --hard`、`git clean -fd` 等破坏性命令。
- Docker 操作仅限本项目定义的容器、镜像、卷和网络。
- 对外发布、推送远程仓库或部署前必须征得用户同意。

# 多 Agent 协作治理

## 主 Agent 职责

主 Agent 是本项目的技术负责人和唯一调度者，负责理解需求、拆分任务、冻结契约、
分配文件所有权、解决冲突、汇总风险和最终验收。子 Agent 的结论属于证据，
不能替代主 Agent 对真实 diff 和验证输出的检查。

## 工作流

1. 修改代码前读取 `README.md`、相关目录的 `AGENTS.md` 和活动任务卡。
2. 非简单任务在 `tasks/active/` 创建任务卡，写明范围、非范围、验收标准和风险。
3. 跨模块交互先在 `contracts/` 定义共享契约，再并行实现。
4. 分派子任务时必须写明目标、可修改文件、只读文件、验收命令和禁止事项。
5. 同一时间一个文件只能有一个实施负责人；文件范围重叠时改为串行。
6. 子 Agent 的阻塞和结果通过原生线程返回；需要跨会话保留时，同时使用
   `scripts/agent_bus.py` 写入持久消息箱。
7. 验收完成后使用 `scripts/complete_task.py <task-id>` 执行完成门，
   不要手工移动任务卡。

小且不可并行的改动可由主 Agent 直接完成，不为形式创建子 Agent。

## 角色与文件所有权

- `backend`：`src/app/**` 中的 API、模型、配置和服务实现。
- `tester`：`tests/**` 中的单元、集成、回归和缺陷复现。
- `security_reviewer`：只读审查认证、授权、输入、网络访问、敏感数据和依赖风险。
- `docs_writer`：`docs/**` 和主 Agent 明确授权的 README 内容。
- `frontend`：当前项目尚无前端目录；只有主 Agent 明确创建并授权具体目录后才可修改。
- 主 Agent：`contracts/**`、`tasks/**`、`.codex/**`、根 `AGENTS.md`。
- `coordination/**` 只能通过 `scripts/agent_bus.py` 写入；已有消息不可修改。

上述所有权是协作约束，不是操作系统 ACL。每个子 Agent 返回后，主 Agent 必须检查实际 diff，
发现越界修改时停止集成并报告。

## Agent 通信

实时通信使用 Codex 原生子 Agent 线程。阻塞、请求、决策和关键结果需要持久化时使用：

```powershell
python .\scripts\agent_bus.py send --from-agent tester --to-agent backend `
  --task example-task --type request --subject "确认错误码" `
  --body "请确认无效测试目标的响应错误码。"
```

非主 Agent 之间的消息自动抄送主 Agent。消息中禁止包含密钥、令牌、真实用户数据或完整敏感日志。
消息箱只用于审计和跨会话恢复，不能自动唤醒 Agent，也不替代原生线程通信。

## 人类审批边界

以下操作必须暂停，在 `tasks/approvals/` 创建审批卡，并取得用户针对具体目标的明确批准：

- 删除、覆盖或批量迁移重要数据；
- 生产数据库写入、不可逆迁移，或连接、部署、重启生产环境；
- `git push`、合并或关闭 PR、创建发布、发布软件包；
- 强制推送、硬重置、清理未跟踪文件、删除分支或标签；
- 创建付费资源、修改云基础设施或执行 `terraform apply/destroy`；
- 读取、导出、轮换或写入密钥、令牌和真实用户敏感数据；
- 向外部人员或系统发送消息、邮件、工单或 webhook；
- 安装新的生产依赖或启用新的外部服务；
- 明显扩大任务范围、改变公开接口或关键安全策略。

审批卡必须包含操作、对象、理由、影响、可逆性、回滚方案和不执行的后果。
一次批准不能解释为永久授权。

项目规则是纵深防御，不是不可绕过的系统安全边界。开始敏感任务前必须确认 Codex
界面权限不是“完全访问/危险完全访问”；会话级实时权限可能覆盖项目和自定义 Agent 默认值。

## 子 Agent 交付格式

```text
完成内容：
修改文件：
验证及结果：
关键假设：
风险与未完成项：
需要主 Agent 决策：
```

## 最终验收

主 Agent 宣布完成前必须检查最终 diff、逐项核验任务卡、运行与风险相称的测试，
确认没有未解决的决策请求或待审批操作，并运行：

```powershell
python .\scripts\complete_task.py <task-id> --check
```

测试或构建失败、必要验证未运行且没有合理说明时，不得声称任务完成。
